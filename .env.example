# LinkedIn Credentials
# These are required for authentication
# Replace the placeholders with your own credentials before running (DO NOT commit real creds)
LINKEDIN_EMAIL=your-email@example.com
LINKEDIN_PASSWORD=your-secure-password

# Configuration
# Optional: Path to config file (YAML or JSON)
CONFIG_PATH=./config.yaml

# Browser Settings
# Set to false to see the browser window (useful for debugging)
BROWSER_HEADLESS=true

# Logging
# Options: debug, info, warn, error
LOG_LEVEL=info
